"use client";
import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import { Users, FileText } from "lucide-react";
export default function P() {const router=useRouter();const [cls,setCls]=useState<any[]>([]);const [l,setL]=useState(true);useEffect(()=>{fetch("/api/classes").then(r=>r.json()).then(d=>{setCls(d.classes||[]);setL(false)});},[]);if(l) return <div className="flex justify-center py-16"><div className="w-8 h-8 border-2 border-primary border-t-transparent rounded-full animate-spin"/></div>;return (<div className="space-y-6 animate-fade-in"><div><h1 className="text-2xl font-bold text-gray-900">My Classes</h1></div><div className="grid md:grid-cols-2 lg:grid-cols-3 gap-6">{cls.map(c=>(<div key={c.id} onClick={()=>router.push("/student/classes/"+c.id)} className="card-premium p-6 cursor-pointer hover:shadow-md"><span className="text-xs font-medium text-primary bg-primary/5 px-2 py-0.5 rounded-full">{c.code}</span><h3 className="text-lg font-semibold mt-3 mb-2">{c.name}</h3><p className="text-sm text-gray-500 mb-4 line-clamp-2">{c.description||c.category}</p><div className="flex items-center gap-4 text-xs text-gray-500 pt-4 border-t border-gray-100"><span className="flex items-center gap-1"><Users className="w-3.5 h-3.5"/>{c._count?.students||0}</span><span className="flex items-center gap-1"><FileText className="w-3.5 h-3.5"/>{c._count?.modules||0}</span></div></div>))}</div></div>);}
