"use client";
import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
export default function P() {const router=useRouter();const [cls,setCls]=useState<any[]>([]);const [l,setL]=useState(true);useEffect(()=>{fetch("/api/classes").then(r=>r.json()).then(d=>{setCls(d.classes||[]);setL(false)});},[]);if(l) return <div className="flex justify-center py-16"><div className="w-8 h-8 border-2 border-primary border-t-transparent rounded-full animate-spin"/></div>;return (<div className="space-y-6 animate-fade-in"><div><h1 className="text-2xl font-bold text-gray-900">My Students</h1><p className="text-gray-500 mt-1">View students in your classes</p></div><div className="space-y-4">{cls.map(c=>(<div key={c.id} className="card-premium p-6"><div className="flex justify-between mb-4"><div><span className="text-xs font-medium text-primary bg-primary/5 px-2 py-0.5 rounded-full">{c.code}</span><h2 className="text-lg font-semibold mt-1">{c.name}</h2></div><span className="text-sm text-gray-500">{c._count?.students||0} students</span></div><button onClick={()=>router.push("/teacher/classes/"+c.id)} className="text-sm font-medium text-primary">View Details →</button></div>))}</div></div>);}
