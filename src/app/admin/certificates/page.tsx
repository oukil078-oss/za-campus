"use client";
import { useEffect, useState } from "react";
export default function P() {
  const [certs,setCerts]=useState<any[]>([]);const [l,setL]=useState(true);
  useEffect(()=>{fetch("/api/certificates").then(r=>r.json()).then(d=>{setCerts(d.certificates||[]);setL(false)});},[]);
  if(l) return <div className="flex justify-center py-16"><div className="w-8 h-8 border-2 border-primary border-t-transparent rounded-full animate-spin"/></div>;
  return (<div className="space-y-6 animate-fade-in"><div><h1 className="text-2xl font-bold text-gray-900">Certificates</h1><p className="text-gray-500 mt-1">All issued certificates</p></div><div className="card-premium overflow-hidden"><div className="overflow-x-auto"><table className="w-full text-sm"><thead className="bg-gray-50 border-b border-gray-100"><tr><th className="text-left px-6 py-3 font-medium text-gray-500">ID</th><th className="text-left px-6 py-3 font-medium text-gray-500">Student</th><th className="text-left px-6 py-3 font-medium text-gray-500">Class</th><th className="text-left px-6 py-3 font-medium text-gray-500">Score</th><th className="text-left px-6 py-3 font-medium text-gray-500">Date</th><th className="text-left px-6 py-3 font-medium text-gray-500">Status</th></tr></thead><tbody>{certs.map(c=>(<tr key={c.id} className="border-b border-gray-50 hover:bg-gray-50/50"><td className="px-6 py-3 font-mono text-xs text-gray-500">{c.certificateNumber}</td><td className="px-6 py-3 font-medium">{c.student?.firstName} {c.student?.lastName}</td><td className="px-6 py-3 text-gray-500">{c.class?.name}</td><td className="px-6 py-3 font-semibold">{c.score}/20</td><td className="px-6 py-3 text-gray-500">{new Date(c.issueDate).toLocaleDateString()}</td><td className="px-6 py-3"><span className={`text-xs font-medium px-2 py-0.5 rounded-full ${c.status==="ISSUED"?"bg-green-50 text-green-700":"bg-red-50 text-red-700"}`}>{c.status}</span></td></tr>))}</tbody></table></div></div></div>);
}
